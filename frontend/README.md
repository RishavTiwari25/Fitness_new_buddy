Frontend (React + Vite)

Run instructions (PowerShell):

1. cd frontend
2. npm install
3. npm run dev

Open http://localhost:5173/ (default Vite port) and use the forms to signup/login. Backend must be running (port 4000).
